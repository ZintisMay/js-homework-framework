// Declare a variable named "num" using "var", make it equal to -> 210

var num = 210;

// Declare a variable named "fruit" using "let", make it equal to -> "apple"

let fruit = "apple";

// Declare a variable named "building" using "const", make it equal to -> "skyscraper"

const building = "skyscraper";

function add(a, b) {
  return a + b;
}

// DO NOT TOUCH ANYTHING BELOW THIS LINE

module.exports = {
  num,
  fruit,
  building,
  add,
};
